@model IEnumerable<YaseinPortfolio.Models.ContactMeByEmail>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ContactMeByEmailEmail)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ContactMeByEmailSubject)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ContactMeByEmailEmail)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ContactMeByEmailSubject)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ContactMeByEmailId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ContactMeByEmailId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ContactMeByEmailId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
